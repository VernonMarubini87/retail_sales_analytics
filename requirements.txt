pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
joblib>=1.3
jupyter>=1.0

# Optional — 04_sales_forecasting.ipynb auto-detects each of these and skips
# gracefully with a printed message if any is missing, so the notebook runs
# end-to-end either way.
xgboost>=2.0        # extra ML model in the comparison
statsmodels>=0.14    # enables SARIMA
prophet>=1.1          # enables Prophet
